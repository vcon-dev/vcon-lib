name: vcon
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
    # Install the project (and its dependencies) from pyproject.toml.
    # pyproject.toml / poetry.lock are the single source of truth for deps;
    # there is no committed requirements.txt to drift out of date.
    - .
